# Phase 1: Migrate tsup to rolldown — Discussion Log

**Date:** 2026-03-28
**Mode:** Interactive (discuss)
**Advisor mode:** Off

---

## Gray Areas Presented

3 gray areas identified:
1. DTS generation
2. Config format
3. Output parity

**User selected:** All 3

---

## Discussion

### Area 1: DTS generation

**Question:** How should .d.ts type declarations be generated?

| Option | Description |
|--------|-------------|
| rolldown-plugin-dts (Recommended) | A rolldown plugin specifically for generating .d.ts files during the build. Keeps everything in one build step. |
| tsc --emitDeclarationOnly | Run tsc as a separate build step. Well-proven, uses existing tsconfig.json. |
| You decide | Let downstream researcher/planner decide based on what's currently stable. |

**User selected:** rolldown-plugin-dts (Recommended)

**Follow-up:** None needed — decision is self-contained.

---

### Area 2: Config format

**Question:** Should the rolldown build use a config file or CLI flags?

| Option | Description |
|--------|-------------|
| rolldown.config.ts file (Recommended) | Create a rolldown.config.ts with all build options. Cleaner package.json, easier to extend later. |
| CLI flags only | Keep build config as CLI flags in package.json scripts, matching the current tsup approach. |
| You decide | Let Claude decide based on what rolldown supports best. |

**User selected:** rolldown.config.ts file (Recommended)

**Follow-up:** None needed — decision is self-contained.

---

### Area 3: Output parity

**Question:** Should the output file names and paths stay exactly the same as the current tsup build?

| Option | Description |
|--------|-------------|
| Exact same output (Recommended) | Output files must be named and structured identically: dist/index.js, dist/index.cjs, dist/index.d.ts. No changes to package.json exports. |
| Rolldown defaults + update exports | Allow rolldown's default naming conventions. Update package.json exports to match whatever rolldown produces. |

**User selected:** Exact same output (Recommended)

**Follow-up:** None needed — decision is self-contained.

---

## Summary

All 3 areas discussed. User confirmed "Create context" without revisiting any decisions (revisit was initially selected but user clarified they wanted the previous question repeated, then confirmed exact same output again).

---

*Generated by discuss-phase workflow*
